function init(self)
	msg.post("/camera#camera", "acquire_camera_focus");
	msg.post("@render:", "use_camera_projection");
end

function on_message(self, message_id, message, sender)
	-- shake camera when a player loses
end
